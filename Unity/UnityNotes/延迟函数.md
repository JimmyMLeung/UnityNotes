# 延迟函数

Invoke（....）//传入无参无返回值的方法，与延迟时间；

‍

InvokeRepeating() //传入无参无返回值的方法，与延迟时间，还有间隔时间；

‍

CancelInvoke（）\\\不传入参数，取消脚本上的所有延迟函数，传入字符串，取消指定延迟函数；

‍

## 注！⚠️

延迟函数,只有在对象销毁，和脚本移除时才停止，要想在失活时取消延迟函数：

```c#
void OnDisable(){
	CancelInvoke(???);
}
```

‍
